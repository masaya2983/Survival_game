<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  		<h1>サバゲーフィールド</h1>
    	<%= render 'layouts/errors', obj: @field %>
  		<%= render 'form', field: @field %>
  		<%= link_to 'Show', @field, class: "book_#{@field.id}" %> |
  		<%= link_to 'Back', fields_path, class: "back" %>
  </div>
</div>
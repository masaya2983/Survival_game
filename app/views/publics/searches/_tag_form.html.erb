<% if user_signed_in? %>
  <div class="search_form">
    <%= form_with url:tag_path, local: true,method: :get do |f| %>
     <%= f.collection_select :tag, Tag.all, :id, :name %>
      <%= f.submit "ジャンル検索" %>
      <% end %>
  </div>
<% end %>